# 1단계 추출 파이프라인 의존성
# 버전은 명세 메모대로 하드코딩하지 않고 설치 시점 최신 안정 버전 사용.
# (필요 시 pip freeze로 lock 파일 별도 생성)

mineru[all]        # 추출 엔진. Apple Silicon에서 MLX 백엔드 가속.
PyMuPDF            # fitz — 페이지 고DPI 래스터화 + 좌표 계산

# 로컬 번역 서버(translate_server.py)용 — 아래는 보통 mineru[all] 이 함께 설치하지만
# 명시해 둔다. NLLB-200-distilled-600M 모델은 첫 실행 시 자동 다운로드(~2.4GB).
transformers
sentencepiece      # NLLB 토크나이저
fastapi
uvicorn
